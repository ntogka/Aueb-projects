//This interface inherits the CharQueue interface and defines an additional method, the min() which return the lexicographically smallest character on the character queue
public interface CharQueueWithMin extends CharQueue {

    public char min();

}
